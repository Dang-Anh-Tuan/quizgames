{
  "name": "quizgame",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-tailwind": "npx tailwindcss -i ./style/input.css -o ./style/output.css --watch",
    "start-json-server": "json-server --watch db.json",
    "dev": "concurrently --kill-others \"npm run start-tailwind\" \"npm run start-json-server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.2.2",
    "tailwindcss": "^3.1.6"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
